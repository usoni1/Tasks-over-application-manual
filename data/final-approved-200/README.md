# Final Approved 200-Point Release

This directory contains the final legally approved 200-point dataset referenced by the paper submission.

Current contents:

- `federal_sentencing_legal_final_dataset_approved.csv`: the final approved review dataset.

Dataset summary:

- source table: `usdo_aa_catalog.research_tam_datasets.federal_sentencing_legal_final_dataset_approved`,
- row count: 200,
- columns: `docket_id` (`string`), `input` (`string`), `output` (`int`).

Constraints:

- Keep only the final approved release here.
- Do not mix exploratory, intermediate, or restricted files into this folder.
- If the approved dataset changes, replace the CSV with the new final approved version.